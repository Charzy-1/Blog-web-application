<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Blog Posts</title>
</head>
<body>
  <h1>Welcome to Charzy's Blog Post web app</h1>
  <% if (posts.length > 0) { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <h2><%= post.title %></h2>
          <p><%= post.content %></p>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No blog posts yet!</p>
  <% } %>
</body>
</html>